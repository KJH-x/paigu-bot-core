# 待决策与需补充信息（DECISIONS）

> **决策日志**。原本单独存放的「用户回复原文」文件已并入本文件各条目的「回复」段，该文件（含末尾空表格）已删除。
> 下方每条的 `已决策`/`回复` 结论即为最终口径；实现状态见 [GAP-ANALYSIS.md](./GAP-ANALYSIS.md) §六 与 [TODOS.md](./TODOS.md)。
> 已定稿口径同步到 [REQUIREMENTS.md](./REQUIREMENTS.md) §3/§4；工程动作见 [TODOS.md](./TODOS.md)。
> 状态：`待决策` · `待提供` · `已决策` · `🟡待确认`（已决策但有一处细节需再确认）。

---

## A. 业务口径

### A-1 减均 / 搜索 / 调价公式 `已决策`（2026-09-17，减均修订）
- **结论**：
  - **折扣最大化**、**调价**两条：按原建议口径通过（见 REQUIREMENTS §4.2/§4.3）。
  - **减均改为**：`B` = Σ各包**实付价**（优惠后）；`C` = **无折扣商品总价**；`G` = 已认购特典价合计；**总优惠额 `D = C − B + G`**；按**各商品自身标价**加权分摊 `D`；**只允许 2 位小数、四舍五入 + 残差校正**；**校验 `Σ商品减均后价 + G = B`**。
- 🟡**待确认**：`C` 是否**不含**特典价（我的理解：不含，`C` = 实购商品标价合计）；加权权重基数用 `C`。
- **影响**：`src/settlement/engine.rs`、`src/planner/**` + 全部单测。

### A-2 特典「实际认购」判定 `已决策`（2026-09-17）
- **结论**：认购在**排谷阶段**按「**成几开几**」——`granted_X = min(认购数_X, 实际成包数 P)`；超出部分**排失败/掉落**（不付款、不参与减均、不归属任何人）。
  - 3A+2B 认购、成 2 包 → `G=2A+2B`；成 3 包 → `G=3A+2B`。
- 🟡**待确认**：`P`（实际成包数）取自 ordering 引擎的排谷结果（唯一来源）。
- **影响**：`G` 与减均、应付金额。

### A-3 下单包模型 `已决策`（2026-09-17）
- **结论**：**一包 = 一份订单**；**不限制包数上限**；**包与商品认购之间不设任何校验联系**（两者解耦）；**特典不花钱**，下单时包总价**不计特典价格**。
- 🟡**待确认**：同一人可出现在多个包（因「无校验联系」，默认允许）。

### A-4 商品基础单价表 `已决策`（2026-09-17）
- **结论**：**无固定价目表，价格一律由管理员填写**（可编辑价格表）。通行证辅助规则：单价 **25/个**；**整盒 = 25 × 盒内个数**（非固定 175）；多数角色含精一/精二，未注明默认两件同购 = **50**，仅精一 = **25**；人事部简历总价视内含角色；风尚速递每角色 1 份；贴纸包内容不定 —— 均管理员填写。
- **影响**：需新增**管理员可编辑价格表**并接入结算 UI（原 T-01）。
- ⚠️ **部分已被取代（2026-09-24，见 G-6/G-7）**：`box_size`/`variants[].pieces` 建模与「整盒 = 25 × 盒内个数」的目录表达**已废弃**；改为商品级仅原价、变体 A/B、整盒为独立种类。**「无固定价目表、管理员填写」结论继续有效**。

### A-5 阶段时间窗 `已决策`（2026-09-17，浮动）
- **结论**：Phase 0 **手动开团**；Phase 1 约**当日 20:00**；Phase 2 约**当日 22:00**；持续 **3 天**；随后**结算**；随后**下单锁定**。时间浮动、以管理员手动为准。

---

## B. 接入与安全

### B-1 `reply_enabled` `已决策`（2026-09-17）
- **结论**：**保持关闭**，直至功能验证通过且**管理员手动开启** → 需提供**运行时热切换开关**（非改文件重启）。

### B-2 成员 user_id 映射 `已决策`（2026-09-17）
- **结论**：**按 NapCat 拉取名单**（`get_group_member_list`，每日 19:00 已实现）建立昵称↔QQ号。

### B-3 白名单口径 `已决策`（2026-09-17）
- **结论**：**仅按群白名单**；不启用成员级白名单。

---

## C. 存储 / 部署

### C-1 Cloudflare 远程展示 `已决策`（2026-09-17）
- **结论**：**暂不部署**（不接线 R2/Pages）。展示页 `remote` 数据源保留接口，待以后启用。

### C-2 快照格式 `已决策`（2026-09-17，改）
- **结论**：**单一 JSON 文件**，内容 = **原始消息数据** + **计算结果缓存** + **计算版本** + **计算时间**。取代「目录式」与「zip」方案。

### C-3 原始事件 `已决策`（2026-09-17，改）
- **结论**：**保留所有成员原始事件**（接入事件溯源，落盘）。

### C-4 旧栈去留 `已决策`（2026-09-17，改：由「隔离」改为「删除」）
- **结论**：**去除旧账**（删除弃用旧栈）。
- 🟡**待确认删除清单**（破坏性操作，确认后执行）：
  - `src/config.rs`、`src/app_state.rs`
  - `src/repo/**`、`src/services/**`、`src/ws/**`、`src/publisher/**`
  - `src/api/{routes,admin_routes,public_routes,webhook_routes}.rs`
  - `src/inbound/**`（仅被旧栈引用）
  - 旧子命令 `serve`（`src/simulation/chat_server.rs`）；`simulate` 按 D-4 迁移到新栈后一并删除旧实现
  - `Cargo.toml` 随之移除 `sqlx`（Postgres）等旧依赖

### C-5 `MessageStore` 方法 `已决策`（2026-09-17，改）
- **结论**：**为本地数据增加所需方法**（`query`/`update`/`delete` 等细粒度方法），不再用「读全量→replace_all」。

---

## D. 功能范围

### D-1 管理员斜杠命令 `已决策`（2026-09-17，改）
- **结论**：**落地执行**，并提供**开关（热切换）**控制是否启用命令执行。

### D-2 `Modify` 改单 `已决策`（2026-09-17，改）
- **结论**：**允许自由改单**；权限 = 普通成员**只能改自己**，管理员**可改群内任意指定人**。

### D-3 LLM 供应商/模型 `未回复`（维持默认）
- **现状**：`https://api.deepseek.com` · `deepseek-flash` · `DEEPSEEK_API_KEY`。**如需更改请补充**。

### D-4 `simulate`/`serve` 子命令 `已决策`（2026-09-17，改）
- **结论**：**统一到新栈**（旧离线 CLI / 旧聊天服务器路径并入新 Gateway→Pipeline 管道）。

---

## 需你补充的信息（汇总）

| # | 需要 |
|---|---|
| A-1 🟡 | 确认 `C` 不含特典价、权重基数 = `C` |
| A-2 🟡 | 确认 `P` 取自排谷成包数 |
| A-3 🟡 | 确认同一人可在多个包 |
| C-4 🟡 | 确认旧栈**删除清单**（见上） |
| D-3 | 确认 LLM 模型名（默认 `deepseek-flash`） |
| A-4 | 管理员价格表的**录入入口**：**标价表随「商品目录」**（`round.items`），添加商品时**手工确认件数与价格** ✅ 2026-09-17 定 |

---

## E. 月行水上-更新 数据口径（2026-09-17 定稿）

- **权威数据**：`simulation-corpus/real-xlsx/月行水上-更新/月行水上-更新.xlsx`（用户提供；`*.xlsx` gitignored）。**忽略「折后价」列**（本次不做折扣）；不调价（用标价，忽略「调价」列）。
- **成团规则**：拼团一律「**成几开几**」——`成团数 = ⌊认购件数 / 每盒件数⌋`，**不足整盒 → 不成团 → 不买**。**本次无「包盒尾」**（故所有不足整盒项一律不买）。
- **本次未成团（不买）**：人事部简历SP（仅结城理×3）、特典拼第二套/第三套。
- **特典「整套」**：**人工数据，不入自动计算**（自动部分排除，用户手动处理）。
- **通行认证SP（A 类，拼团）**：
  | 变体 | 件数 | 标价 |
  |---|---|---|
  | 结城理 / 岳羽由加莉 / 埃癸斯 | 2 | ¥50 |
  | 虎狼丸 | 1 | ¥25 |
  | 合计 | **7 件 = 1 整盒** | **¥175** |
  → 标价 = `¥25 × 件数`；认购 4 变体全认领 → **成 1 盒**。
- **风尚速递SP**：共 **3 份 = 拼成 1 份 + 单领 2 份**（¥15/份）。
- **特典（两个 section）**：
  - section1「特典（259元）」：计数 **11 份**，每份 **¥12**；
  - section2「特典卡组-校园凭证」：拼成 **1 套 = 1 份**，**¥12**；
  - 合计 **12 份 = ¥144** → 即 **G = 144**（与 A-2 `granted=min(claims=12, P=12)=12` 一致）。
- **下单包数 P = 12**（Sheet2「第1单…第12单」）。
- **单领（报盒）部分**：Sheet2 的每单价（280/273/…/266）与总价 3270 **仅作自动结果的验算参考**，不作为计算输入。
- **Sheet3 特典价合计 144** 佐证 G。

---

## F. 实施状态（2026-09-17，Wave 1-4 全部落地）

| 决策 | 状态 | 落地 |
|---|---|---|
| A-1 减均 | ✅ 已实现 | `settlement::evaluate`（`D=C-B+G`、按件加权、残差校正、校验式） |
| A-2 成团/特典 | ✅ 已实现 | `GiftTier.claimed` + `granted=min(claimed,P)`；月行水上-更新 G=¥144 |
| A-3 下单包 | ✅ 已实现 | 一包=一单、无上限、包与认购解耦、特典免费不计包价 |
| A-4 标价表 | ✅ / ⚠️ 部分取代 | 标价入 `round.items`，editor 页可编辑；`box_size`/`pieces` 已被 SPEC-UPDATE 2026-09-24 取代（见 G-6/G-7） |
| A-5 阶段时间 | ✅ 已记录 | 写入 REQUIREMENTS §2（浮动，以管理员手动为准） |
| B-1 reply_enabled | ✅ 已实现 | `POST /api/config/reply` 热切换；默认关闭 |
| B-2 名单拉取 | ✅ 已实现 | NapCat `get_group_member_list` + 每日 19:00（待实机验证） |
| B-3 群白名单 | ✅ 已实现 | 仅按群 |
| C-1 远程展示 | ⏸ 暂不部署 | `Public*` 视图模型保留 |
| C-2 单 JSON 快照 | ✅ 已实现 | `SnapshotFile` + `export_file/import_file` |
| C-3 原始事件 | ✅ 已实现 | `data/events/<round>.jsonl` + `GET /api/events` |
| C-4 删旧栈 | ✅ 已实现 | `dev` 分支删除 + 回归全绿 + 合并 `master` |
| C-5 细粒度方法 | ✅ 已实现 | `MessageLog::{query,update,delete}` |
| D-1 管理员命令 | ✅ 已实现 | 热开关 + `/开团 /锁位 /结团 /状态 /导出` |
| D-2 改单 | 🟡 部分 | 自助改单已实现；**管理员改任意人**待补目标语法（T-14） |
| D-3 LLM 模型 | ❓ 未回复 | 维持 `deepseek-flash`（如需变更请说明） |
| D-4 子命令统一 | ✅ 已实现 | `serve` 并入新栈；`simulate` 暂留旧 verifier（T-24） |

---

## G. 2026-09-24 追加口径（SPEC-UPDATE U1–U9；实现见正文与实现注记）

> 权威来源 [SPEC-UPDATE-2026-09-24.md](./SPEC-UPDATE-2026-09-24.md)；正文已按现行口径更新。每条 1 行。

| # | 决策（现行） |
|---|---|
| G-1 | **Stepper 高亮 = 当前所在模块**（非当前阶段）；阶段进度用**更弱元素**表达；**开团 = `/round`**（轮次与商品）；其余配置归 **`/settings`**，**不进 Stepper**。 |
| G-2 | `round_id` = **稳定标识**（消息日志/快照/remote/`RoundId`）；`title` = **展示名**；二者**可不同**。 |
| G-3 | **轮次库**：`config/app.json` 只留 `active_round_id`；轮次数据 `data/rounds/<round_id>.json`；`GET/POST /api/rounds`、`activate`、`check`、`DELETE`。 |
| G-4 | **切换与重放解耦**：`activate` 模式 `continue`（**默认**，无状态时等价 `fresh`）/`fresh`/`replay`；重放是独立自动计算，不与切换绑定。 |
| G-5 | **成员 CN**：`members.cn_overrides[{user_id,cn,aliases}]`，绑定 QQ；回退 **CN → 归一化昵称 → user_id**；用于匹配与**结算表/账单人名**。 |
| G-6 | **商品模型**：商品级仅**原价**；**仅变体**有 `unit_price_cents`(A)+`adjust_cents`(B)，最终价 C=A+B；**`box_size` 与 `variants[].pieces` 已从配置模型移除**。 |
| G-7 | **种类与 class**：`拼团/单领/整盒/特典`；`class` **自动推导**（有变体⇒A 受限；无变体⇒B）；**整盒为独立种类**（默认进单领队列）。 |
| G-8 | **first-match 与失败澄清**：只报角色名→目录序**第一个可拼团商品**；全 fail→**LLM 澄清**→**`ParseOverride` 事件持久化**并**被重放消费**。 |
| G-9 | **整盒/包尾**：整盒→单领队列；`fullbox` 保留为拼团策略；**包尾→拼团并在结算强制成盒**；**列=盒序号**，锁定列 = 申报变体**最大列序 + 1**；冲突外的前序未成盒普通认购**自动滑入**。 |
| G-10 | **前端**：`/round`、`/settings` 新页；结算页**隐藏调价录入**（仅展示调后价）；消息日志编辑器为排谷界面**折叠面板**；时间窗显示 `周几+今天/明天/后天/X天前后`。 |
| G-11 | **接口**：`GET /api/workflow` 增 `phases`；`POST /api/items/suggest-aliases` 前端已接入、后端已实现（`src/api/item_routes.rs`）（见 TODOS W-G2-02）；`VariantConfig.adjust_cents` 已声明并落库（W-G2-01）。 |

> **取代关系**：原 **A-4**（`round.items` 含 `box_size`/`pieces` 与「整盒 = 25 × 盒内个数」的建模）**已被 SPEC-UPDATE 2026-09-24 §U6 取代**：商品级只留原价、变体承载 A/B、移除 `box_size` 与 `variants[].pieces`、整盒改为**独立种类**。原 A-4 的「无固定价目表、管理员填写」结论**继续有效**。
